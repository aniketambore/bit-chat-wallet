name: bit_chat_wallet
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  bdk_flutter: ^0.27.2
  flutter_bloc: ^8.1.2
  flutter_secure_storage: ^8.0.0
  qr_flutter: ^4.0.0
  equatable: ^2.0.5
  nostr_tools: ^1.0.7
  hive: ^2.2.3
  path_provider: ^2.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
